# ── Future Discord integration (server-only) ─────────────────────
# These are placeholders for a later step. Do NOT implement yet.
# Never expose secrets with a NEXT_PUBLIC_ prefix or in client components.
# Resolve user 920627516694200360 via a server route / server action.

# DISCORD_BOT_TOKEN=
# DISCORD_GUILD_ID=
