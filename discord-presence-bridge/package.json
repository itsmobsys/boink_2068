{
  "name": "discord-presence-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny persistent Discord Gateway → HTTP presence bridge for the Boink website. Watches one user, serves one status.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test src/presence.test.js src/server.test.js"
  },
  "dependencies": {
    "discord.js": "^14.18.0"
  }
}
